<!DOCTYPE html>
<html>

<head>
    <title>Facture</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
            margin: 0;
        }

        p {
            padding: 0;
            margin: 0;
            font-size: 13px;

        }

        h1 {
            text-align: center;
            font-size: 20px;
            color: #1EABDC;

        }
        .company-info {
            font-weight: bold;
            text-align: center;
            width: 100px;
        }

        .logo img {
            max-height: 80px;
            margin-bottom: 5px
        }

        .clientInfo {
            position: absolute;
            top: 50px;
            right: 0;
            
        }

        .facture
        {
            text-align: right;
            margin-top:35px;
            min-height: 600px;
        }
        .facture p{
            font-size:18px;
            padding-bottom: 3px;
        }

        .facture p span {
            background-color: #1EABDC;
                padding: 2px;
                color: white;

        }
        .total{
            text-align: center;
            margin-top: 25px;
             background-color: #1EABDC;
                color: white;
            
        }
        .total p{
            font-size:21px;
            padding:10px;

        }
        .total p span{
            color: red;
        }
        .tva {
            font-size:15px;
            text-align: right;
            margin-top: 35px
            
        }
        .tva span{
            color: red;
        }
    </style>
</head>

<body>
    <div class="header">
        <div class="company-info">
            <div class="logo">
                <img src="{{ absolute_url(asset('logo/xtraLogo.png')) }}" alt="Logo de xtra copy">
            </div>
            <p>Rue de l'Yser 11</p>
            <p> 7500 Tournai</p>
            <p>Belgique</p>
        </div>
        <div class="clientInfo">
            <p>Vente au comptoir</p>
        </div>
    </div>

    <h1>Facture</h1>
    <p>Date: {{data.date}}</p>
    <div class="facture">
        {{data.texte|raw}}
    </div>
    <p class="tva">La tva est de <span>{{ (data.total * 0.21)|number_format(2) }}€</span></p>
    <div class="total">
    
    <p>Le total tva comprise est de <span>{{data.total|number_format(2)}}€</span></p>
    
    </div>
</body>

</html>